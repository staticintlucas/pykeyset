[package]
name = "pykeyset"
version = "0.1.1"
authors = ["Lucas Jansen"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
keyset = "0.3.0"
pyo3 = { version = "0.21.0-beta.0" }

[build-dependencies]
built = { version = "0.6", default-features = false }
