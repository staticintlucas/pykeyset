[package]
name = "pykeyset"
version = "0.1.0-alpha0"
authors = ["Lucas Jansen"]
edition = "2021"

[lib]
name = "pykeyset"
crate-type = ["cdylib"]

[dependencies]
keyset = { git = "https://github.com/staticintlucas/keyset-rs" }
pyo3 = "0.18"

[build-dependencies]
built = { version = "0.6", default-features = false }

[package.metadata.maturin]
name = "pykeyset._impl"
