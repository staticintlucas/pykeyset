[build-system]
requires = ["setuptools >= 36.2.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py36', 'py37', 'py38']
include = '\.pyi?$'
# Ignore all autogenerated files
exclude = '''
      ^/versioneer.py$
    | ^/keyset/__init__.py$
    | ^/keyset/version.py$
'''
